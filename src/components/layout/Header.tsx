import React from "react";
// This component is now integrated into MainLayout.tsx
// Keeping the file for backward compatibility but it's no longer used

const Header = () => {
  return null;
};

export default Header;
